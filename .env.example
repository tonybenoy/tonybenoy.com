# Environment Configuration Example
# Copy this file to .env.local, .env.dev, or .env.prod and customize

# Application Environment (local|dev|prod)
APP_ENV=local

# Logging Level (DEBUG|INFO|WARNING|ERROR)
LOG_LEVEL=DEBUG

# Network Configuration
NGINX_CONFIG=app-dev.conf
HTTPS_PORT=443

# GitHub API Settings
GITHUB_USERNAME=tonybenoy
# Optional: GitHub token for higher API rate limits
GITHUB_TOKEN=

# Security Settings
# For local: ["*"] 
# For dev: ["http://localhost","http://127.0.0.1"]
# For prod: ["https://yourdomain.com"]
CORS_ORIGINS=["*"]

# For local: ["*"]
# For dev: ["localhost","127.0.0.1"] 
# For prod: ["yourdomain.com","www.yourdomain.com"]
ALLOWED_HOSTS=["*"]

# Development Mode - Volume Mounting
# For local development with live reload: ./src
# For dev/prod (no mounting): /tmp/empty
CODE_MOUNT=./app

# Redis Settings (usually don't need to change)
REDIS_HOST=redis_db
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_CACHE_TTL=3600

# Example configurations:

# .env.local (Development with live reload):
# APP_ENV=local
# LOG_LEVEL=DEBUG
# CODE_MOUNT=./app
# CORS_ORIGINS=["*"]
# ALLOWED_HOSTS=["*"]

# .env.dev (Development environment):
# APP_ENV=dev  
# LOG_LEVEL=DEBUG
# CODE_MOUNT=/tmp/empty
# CORS_ORIGINS=["http://localhost","http://127.0.0.1"]
# ALLOWED_HOSTS=["localhost","127.0.0.1"]

# .env.prod (Production environment):
# APP_ENV=prod
# LOG_LEVEL=INFO
# NGINX_CONFIG=app.conf
# CODE_MOUNT=/tmp/empty
# CORS_ORIGINS=["https://tonybenoy.com"]
# ALLOWED_HOSTS=["tonybenoy.com","www.tonybenoy.com"]
EOF < /dev/null